/*
 * PageDetail Messages
 *
 * This contains all the text for the PageDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PageDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PageDetail container!',
  },
});
