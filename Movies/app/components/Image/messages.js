/*
 * Image Messages
 *
 * This contains all the text for the Image component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Image';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Image component!',
  },
});
