/*
 * H2 Messages
 *
 * This contains all the text for the H2 component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.H2';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the H2 component!',
  },
});
